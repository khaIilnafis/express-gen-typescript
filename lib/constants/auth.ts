/**
 * Authentication-related constants
 */
export const AUTH = Object.freeze({
  TYPES: {
    NONE: "none",
    PASSPORT: "passport",
    JWT: "jwt",
    EXPRESS_SESSION: "express-session",
  },
});
